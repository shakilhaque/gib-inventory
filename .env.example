# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=bank_inventory
DB_USER=postgres
DB_PASSWORD=your_strong_password_here

# JWT Secret (generate with: openssl rand -hex 32)
JWT_SECRET=your_jwt_secret_here

# Frontend port (80 for HTTP)
APP_PORT=80
